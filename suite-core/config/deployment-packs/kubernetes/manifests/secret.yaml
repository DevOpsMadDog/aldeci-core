apiVersion: v1
kind: Secret
metadata:
  name: fixops-secrets
  namespace: fixops
  labels:
    app.kubernetes.io/name: fixops
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  EMERGENT_LLM_KEY: "REPLACE_WITH_YOUR_KEY"
  SECRET_KEY: "REPLACE_WITH_RANDOM_SECRET"
  MONGO_INITDB_ROOT_USERNAME: "fixops"
  MONGO_INITDB_ROOT_PASSWORD: "REPLACE_WITH_SECURE_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_REDIS_PASSWORD"
