{
  "stage": "requirements",
  "input": [
    "fixtures/sample_inputs/requirements/requirements-input.csv"
  ],
  "programs_invoked": [
    "python -m apps.fixops_cli stage-run --stage requirements --input fixtures/sample_inputs/requirements/requirements-input.csv --app life-claims-portal --mode demo"
  ],
  "outputs": [
    "fixtures/expected_outputs/requirements/requirements.json"
  ]
}
