{
  "stage": "deploy",
  "input": [
    "fixtures/sample_inputs/deploy/tfplan.json"
  ],
  "programs_invoked": [
    "python -m apps.fixops_cli stage-run --stage deploy --input fixtures/sample_inputs/deploy/tfplan.json --app life-claims-portal --mode demo"
  ],
  "outputs": [
    "fixtures/expected_outputs/deploy/deploy.manifest.json"
  ]
}
